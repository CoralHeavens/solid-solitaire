import React from "react";
import Spider from "../components/Spider";

const routes = [
    {
        label: 'Spider',
        route: '/spider',
        component: <Spider />,
    }
]

export default routes;
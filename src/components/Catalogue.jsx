import React from "react";

const title = 'Choose your solitaire:';

const Catalogue = () => {
    return (
        <main>
            <h1 className="glitch uppercase text-5xl" title={title}>{title}</h1>

        </main>
    )
}

export default Catalogue;